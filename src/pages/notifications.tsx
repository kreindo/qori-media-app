import { type NextPage } from "next";
import Layout from "../components/layout";

const Notifications: NextPage = () => {
  return <Layout>Dis notification</Layout>;
};

export default Notifications;
